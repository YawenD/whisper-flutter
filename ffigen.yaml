# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterWhisperGgmlBindings
description: |
  Bindings for `src/flutter_whisper_ggml.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_whisper_ggml_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_whisper_ggml.h'
  include-directives:
    - 'src/flutter_whisper_ggml.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
